import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidape_curso/app/modules/home/enderecos/enderecos_page.dart';

main() {
  testWidgets('EnderecosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EnderecosPage(title: 'Enderecos')));
    //  final titleFinder = find.text('Enderecos');
    //  expect(titleFinder, findsOneWidget);
  });
}
